{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    // SQLite connection for local usage (avoids SQL Server 'server' keyword error)
    "DefaultConnection": "Data Source=IPTSYSTEM.db"
  }
}
